<!-- ============= TOP STRIP (ACF Options) ============= -->
    <!-- <?php $msg = function_exists('get_field') ? (get_field('topstrip_message', 'option') ?: '😊 Welcome to ENERG Portal') : '😊 Welcome to ENERG Portal'; ?>
    <div class="topstrip" aria-hidden="true">
        <div class="topstrip__track">
            <?php for ($i = 0; $i < 5; $i++): ?>
                <span class="topstrip__chunk"><?php echo wp_kses_post($msg); ?> <span class="topstrip__sep">•</span></span>
            <?php endfor; ?>
        </div>
    </div> -->



    /* Top strip */
        /* .topstrip {
            background: #e6e6e6;
            border-bottom: 1px solid #dedede;
            overflow: hidden;
            white-space: nowrap
        }

        .topstrip__track {
            display: inline-block;
            padding: 8px 0;
            animation: scroll-x 26s linear infinite
        }

        .topstrip__chunk {
            display: inline-flex;
            gap: 24px;
            align-items: center;
            padding: 0 28px
        }

        .topstrip__sep {
            opacity: .35
        }

        @keyframes scroll-x {
            from {
                transform: translateX(0)
            }

            to {
                transform: translateX(-50%)
            }
        }

        @media (prefers-reduced-motion:reduce) {
            .topstrip__track {
                animation: none
            }
        } */